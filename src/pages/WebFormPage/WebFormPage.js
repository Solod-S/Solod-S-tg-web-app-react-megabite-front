import { WebForm, BackBtn, SectionWrapper } from "../../components";

function WebFormPage() {
  return (
    <SectionWrapper>
      <BackBtn location="/" />
      <WebForm />
    </SectionWrapper>
  );
}

export default WebFormPage;
