export const fetchDeals = () => {
  // return axiosInstance.get(`/favorite/`).then(({ data }) => {
  //   return data;
  // });
  return [];
};

export const deleteDeal = (id) => {
  return [];
};

export const addDeal = (id) => {
  return [];
};

// https://www.youtube.com/watch?v=Na0ZunuSVBQ
